/**
 * Type re-exports from @wopr-network/plugin-types.
 *
 * Plugins import from this local file so there is a single place to
 * update if the canonical package changes.
 */

export type {
  A2AServerConfig,
  A2AToolDefinition,
  A2AToolResult,
  ConfigField,
  ConfigSchema,
  PluginCapability,
  PluginCategory,
  PluginManifest,
  WOPRPlugin,
  WOPRPluginContext,
} from "@wopr-network/plugin-types";
